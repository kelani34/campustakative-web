'use client';

import Header from '@/components/header';

const ProductsHeader = () => {
  return (
    <Header
      title="PRODUCTS"
      text="Our product and services are tailored towards building for the best set of youths."
    />
  );
};

export default ProductsHeader;
