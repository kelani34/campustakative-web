'use client';

import Header from '@/components/header';

const EventsHeader = () => {
  return (
    <Header
      text="Exciting events to provide value to our community. Don’t miss out on these awesome events."
      title="OUR EVENTS"
    />
  );
};

export default EventsHeader;
